# 版本改动日志

## 未发布

### 答辩前功能

- 新增运营员/管理员签到核验页，支持摄像头扫描或上传二维码截图，并调用 V2 签到接口完成核销。
- 完善运营台表单的字段说明、必填标记、提交前校验和错误提示。
- 新增 Playwright 浏览器流程测试，使用有状态 API 替身覆盖“学生登录 → 建立仓位 → 报名 → 抽签后领票 → 运营员签到核验”。
- 新增课程答辩演示脚本，整理演示账号、角色分工、准备步骤、逐段讲解和故障恢复方案。

### 部署与配置

- 链码部署时若本机未安装 Go，自动通过 Fabric `ccenv` 镜像执行 Go 命令，并沿用当前 WSL 用户权限写入依赖目录。
- 修正三个组织的 Fabric CA TLS 证书路径，统一读取 `ca-cert.pem`。
- 调整 Demo 环境变量示例，使本地演示可从 `JWT_SECRET` 派生确定性开发密钥，同时保留生产环境独立密钥要求。

### 工程与文档

- 增加签到页面路由、角色导航入口、活动状态中文显示和签到证明解析工具。
- 增加 `npm run test:e2e --prefix client` 命令及对应 Playwright 配置。
- 更新 README 和 TODO，记录新增页面、测试边界及四项答辩前任务的完成状态。
- 将答辩排障说明整理为 Windows + WSL 通用步骤，并在生产启动时强制校验身份查询与加密密钥。

### 验证

- 前端 `npm run build` 通过。
- 浏览器流程测试已纳入项目；它验证前端交互与状态衔接，不等同于真实 Fabric 网络集成测试。

## v2.1.0 · 2026-09-08

### 前端

- 统一赛事市场、票务大厅、服务兑换、钱包和“我的”页面的响应式布局。
- 调整为白底、浅灰分区、蓝色操作色，减少大面积深色块。
- 优化导航栏和链路信息的居中对齐。
- 完善中文角色、状态、积分类型和服务类型显示。
- 清理无用组件，并改为按需加载 Element Plus。

### 后端与区块链

- 增加 CORS、请求体限制、安全响应头、限流清理和优雅停机。
- 加强 Fabric 多组织 TLS、CA 连接、Gateway 缓存和钱包文件权限管理。
- 移除废弃 V1 路由，统一使用 V2 API。
- 调整活动私有集合背书策略并格式化预测链码。

### 工程与文档

- 重写 README，补充 WSL2 启动、恢复账本、前端文件位置和故障排查说明。
- 更新 TODO 和实现说明。
- 完善 `.gitignore`，忽略依赖、构建产物、数据库、钱包和 Fabric 二进制。
- 新增 `.editorconfig` 和 API 测试。

### 验证

- 前端 `npm run build` 通过。
- 后端测试通过。
- 活动、财务、预测链码的 `go test` 和 `go vet` 通过。
